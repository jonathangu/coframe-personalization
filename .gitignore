# Harness output (regenerated by run_eval.py / plot_results.py).
/results/

# uv / virtualenv (regenerated by `uv sync` / `uv run`). pyproject.toml, uv.lock
# and .python-version ARE committed; the environment itself is not.
.venv/
pyrightconfig.json

# Caches & cruft.
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
.mypy_cache/
.ruff_cache/

reference/
